from django.contrib import admin
from django.urls import path, include
# from .views import create_chat_by_student, chatroom_student_side, create_chat_by_mentor, chatroom_mentor_side
urlpatterns = [
    # path('chatroom_student/<uuid:chat_uuid>/', chatroom_student_side, name="chatroom_student_side"),
    # path('create_chatroom_learner/<uuid:user_uuid>/', create_chat_by_student, name="create_chat_by_student"),
    # path('create_chatroom_mentor/<uuid:user_uuid>/', create_chat_by_mentor, name="create_chat_by_mentor"),
    # path('chatroom_mentor/<uuid:chat_uuid>/', chatroom_mentor_side, name="chatroom_mentor_side"),
]